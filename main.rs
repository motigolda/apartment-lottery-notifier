use requests;
use parser;
use notifier;

fn main(){
    // config the request
    
    // create thread that sends summary once a day
    // while true -
        // perform new request
        // check if new lottery opened
        // notify if something change
}